<!DOCTYPE html>
<html lang="en">
<head>
 <% include partials/head %> 
</head>
<body>
<% include partials/logo-header %>

<div class="container">
        <div class="col-sm-4 col-sm-offset-4">        
            <h1><span class="fa fa-sign-in"></span> Login</h1>    
            <% if (message.length > 0) { %>
                <div class="alert alert-danger"><%= message %></div>
            <% } %>           
            <!-- LOGIN FORM -->
            <form action="/users/login" method="post">
                <div class="form-group">
                    <label>Username</label>
                    <input type="text" class="form-control" name="username" value="Joko">
                </div>
                <div class="form-group">
                    <label>Password</label>
                    <input type="password" class="form-control" name="password" value="Joko2019">
                </div>
        
                <button type="submit" class="btn btn-warning btn-lg">Login</button>
            </form>        
            <hr>        
            <p>Create new account ? <a href="/users/register">Register</a></p>       
        </div>
        </div>
</body>
</html>